<h1>食べたごはんの記録</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@dish_image, url: record_path) do |f| %>
      <%= f.label :dish_id, "料理ID" %>
      <%= f.text_field :dish_id %>

      <%= f.label :filename, "画像ファイル名" %>
      <%= f.text_field :filename %>

      <%= f.submit "登録", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>